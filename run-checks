#!/bin/sh

mkdir -p bin
( cd bin &&  wget https://niemeyer.s3.amazonaws.com/spread-amd64.tar.gz -O spread.tar.gz && tar xzvf spread.tar.gz )

cp ./options target/tasks/snapd-vendor-sync
env
cd target && ../bin/spread -v google:
