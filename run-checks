#!/bin/sh

mkdir -p bin
export PATH=./bin:$PATH
( cd bin && curl -s -O https://niemeyer.s3.amazonaws.com/spread-amd64.tar.gz && tar xzvf spread-amd64.tar.gz )
./actions.sh
cd target && spread -v linode:
