project: spread-cron-lp-build-core

path: /sync

backends:
    linode:
        type:
        key: "$(HOST: echo $SPREAD_LINODE_KEY)"
        systems:
            - ubuntu-16.04-64:
                kernel: GRUB 2

suites:
    tasks/:
        summary: Generic automatable tasks
